# Neovim Configuration

### Plugin management
Plugins managed by Lazy.nvim and all located in lua/plugins/*.lua
Use `:Lazy` to inspect installed plugins

### LSP servers are managed by Mason.nvim
Use `:Mason` to inspect installed LSP (formatter, linter, etc) servers

